// jshint devel:true
(function (window) {

    'use strict';

    // Handle dependencies
    var smoothScroll = window.smoothScroll;
    smoothScroll.init({
      easing: 'easeInOutQuart'
    });

    // Your application's code
}(window));
